import styled from "styled-components";

const MessageDialogStyle = styled.div`
  display: grid;
  width: 376px;
  padding: 12px;
  gap: 22px;

  .title {
    text-align: center;
    font-size: 16px;
    font-weight: 400;
    color: var(--text_primary);
  }
`;

export default MessageDialogStyle;
