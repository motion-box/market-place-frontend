import styled from "styled-components";

const AccountDialogStyle = styled.div`
  display: grid;
  grid-auto-flow: column;
  position: relative;
  width: 374px;
`;

export default AccountDialogStyle;
