import styled from "styled-components";

const MUserOrdersPageStyle = styled.div`
  display: grid;
  width: 100%;
  padding: 64px 0 32px;
  gap: 12px;

  .top_cont {
    padding: 0 16px;
  }

  .cards_cont {
    border-radius: 8px;
    background-color: var(--card_color);
    padding-left: 16px;
  }
`;

export default MUserOrdersPageStyle;
