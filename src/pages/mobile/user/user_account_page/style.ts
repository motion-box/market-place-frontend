import styled from "styled-components";

const MUserAccountPageStyle = styled.div`
  display: grid;
  width: 100%;
  padding: 64px 16px 32px;
  gap: 12px;

  .buttons_cont {
    display: grid;
    gap: 8px;
    width: 100%;
    border-radius: 8px;
    background-color: var(--card_color);
    padding: 8px 0px 8px 8px;
  }
`;

export default MUserAccountPageStyle;
