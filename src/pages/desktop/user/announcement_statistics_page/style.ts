import styled from "styled-components";

const AnnouncemetStatisticsPageStyle = styled.div`
  display: grid;
  width: 100%;
  background-color: var(--card_color);
  padding: 116px 0 0;

  .page_cont {
    display: grid;
    gap: 24px;
    justify-items: center;
  }

  button {
    color: var(--text_primary);
  }
`;

export default AnnouncemetStatisticsPageStyle;
