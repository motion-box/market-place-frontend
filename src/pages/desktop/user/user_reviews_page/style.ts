import styled from "styled-components";

const UserReviewsPageStyle = styled.div`
  display: grid;
  width: 100%;
  background-color: var(--card_color);
  padding: 116px 0 0;
`;

export default UserReviewsPageStyle;
