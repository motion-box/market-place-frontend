import styled from "styled-components";

const UserFavoritePageStyle = styled.div`
  display: grid;
  width: 100%;
  background-color: var(--card_color);
  padding: 116px 0 0;

  .users_container {
    width: 100%;
    display: grid;
    gap: 8px;
  }
`;

export default UserFavoritePageStyle;
