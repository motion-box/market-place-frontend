import styled from "styled-components";

const UserOrdersPageStyle = styled.div`
  display: grid;
  width: 100%;
  background-color: var(--card_color);
  padding: 116px 0 0;

  .content_wrapper {
    width: 100%;
    display: grid;
    gap: 8px;
  }
`;

export default UserOrdersPageStyle;
